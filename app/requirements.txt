# fastapi atualizado de 0.111.0 → >=0.115.0 para puxar starlette>=1.0.1
# e corrigir CVE-2024-47874, CVE-2025-54121, PYSEC-2026-161 detectados pelo pip-audit.
fastapi>=0.115.0
uvicorn[standard]>=0.29.0
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
redis==5.0.4
pydantic==2.7.1

# ── OpenTelemetry — Core ──────────────────────────────────────────────────────
# Pin atualizado de 1.25.0 → 1.42.1 para acompanhar instrumentation 0.63b1.
# A remoção do pin setuptools<80.0.0 também foi possível porque a 0.63b1+
# não usa mais pkg_resources (migrou para importlib.metadata — ver PEP 451).
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1

# ── OTLP Exporter — gRPC → OTel Collector :4317 ─────────────────────────────
opentelemetry-exporter-otlp-proto-grpc==1.42.1

# ── Auto-instrumentação ───────────────────────────────────────────────────────
# 0.63b1 é o companion beta do SDK 1.42.1 (versões sempre caminham juntas).
opentelemetry-instrumentation-fastapi==0.63b1
opentelemetry-instrumentation-sqlalchemy==0.63b1
opentelemetry-instrumentation-redis==0.63b1
